/* ---------------------------------
initialState
--------------------------------- */

import { AuthState } from "../../types";

export const initialState: AuthState = {
  user: null,
  jwt: null,
};
