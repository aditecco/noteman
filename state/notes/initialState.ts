/* ---------------------------------
initialState
--------------------------------- */

import { NotesState } from "../../types";

export const initialState: NotesState = {
  loading: "idle",
  notes: [],
};
